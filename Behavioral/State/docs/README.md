# C# State Design Pattern
The State design pattern lets an object alter its behavior when its internal state changes. The object will appear to change its class.<br>
>State : Alter an object's behavior when its state changes.<br>

## Key idea
- **Context** delegates state-specific behavior to a **State** object.
- Transitions are handled by changing the current state instance.

